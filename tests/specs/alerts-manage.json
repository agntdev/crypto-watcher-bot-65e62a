[
  {
    "name": "Manage alerts with empty watchlist",
    "steps": [
      {
        "send": { "callback": "alerts:manage" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Add coins to your watchlist before setting up alerts."
            }
          }
        ]
      }
    ]
  }
]
